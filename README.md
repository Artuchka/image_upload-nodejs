# image_upload-nodejs

### node js mini-project to train uploading images to the server

back-end stack:
  - node js
  - express => express-fileupload
  - mongodb => mongoose

front-end stack: 
  - react (seems faster to configure than \w vanilla js)
